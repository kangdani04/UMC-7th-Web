const Footer = () => {
    return (
        <nav>
            <div style={({ display: 'flex', justifyContent: 'center' })}>
                <h3>Playlist</h3>
            </div>
        </nav>
    );
};

export default Footer;